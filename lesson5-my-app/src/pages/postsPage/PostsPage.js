import React from 'react';

const PostsPage = () => {
    return (
        <div>
            <button>get posts</button>
            <button>delete all</button>
        </div>
    );
};

export default PostsPage;